,0
0,// Create Timestamp and Duration from .NET DateTimeOffset and TimeSpan
1,var meeting = new Meeting
2,{
3,"Time = Timestamp.FromDateTimeOffset(meetingTime), // also FromDateTime()"
4,Duration = Duration.FromTimeSpan(meetingLength)
5,};
6,
7,// Convert Timestamp and Duration to .NET DateTimeOffset and TimeSpan
8,DateTimeOffset time = meeting.Time.ToDateTimeOffset();
9,TimeSpan? duration = meeting.Duration?.ToTimeSpan();
