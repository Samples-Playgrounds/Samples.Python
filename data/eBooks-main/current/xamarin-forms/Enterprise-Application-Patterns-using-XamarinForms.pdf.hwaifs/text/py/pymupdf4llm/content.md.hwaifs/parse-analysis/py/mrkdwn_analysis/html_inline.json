[{'line': 688, 'html': '<t>` is provided. This collection implements\ncollection changed notification, relieving the developer from having to implement the\n`INotifyCollectionChanged` interface on collections.</t>'}, {'line': 916, 'html': '<t>` classes, provided by Xamarin.Forms, implement the `ICommand`\ninterface, where `T` is the type of the arguments to `Execute` and `CanExecute` .</t>'}, {'line': 926, 'html': "<t>` for each public\nproperty in the view model of type `ICommand` . The `Command` or `Command<t>` constructor requires an\n`Action` callback object that's called when the `ICommand.Execute` method is invoked. The\n`CanExecute` method is an optional constructor parameter, and is a `Func` that returns a `bool` .</t></t>"}, {'line': 926, 'html': "<t>` constructor requires an\n`Action` callback object that's called when the `ICommand.Execute` method is invoked. The\n`CanExecute` method is an optional constructor parameter, and is a `Func` that returns a `bool` .</t>"}, {'line': 955, 'html': '<t>` class to\ninstantiate the command. For example, the following code shows how a `Command<t>` instance is used\nto indicate that the `NavigateAsync` method will require an argument of type `string` :</t></t>'}, {'line': 955, 'html': '<t>` instance is used\nto indicate that the `NavigateAsync` method will require an argument of type `string` :</t>'}, {'line': 964, 'html': "<t>` classes, the delegate to the `CanExecute` method in each\nconstructor is optional. If a delegate isn't specified, the `Command` will return `true` for `CanExecute` .\nHowever, the view model can indicate a change in the command's `CanExecute` status by calling the\n`ChangeCanExecute` method on the `Command` object. This causes the `CanExecuteChanged` event to be\nraised. Any controls in the UI that are bound to the command will then update their enabled status to\nreflect the availability of the data-bound command.</t>"}, {'line': 1003, 'html': '<t>` class, where `T` is\nthe type of the control to which the behavior should apply. These classes provide `OnAttachedTo` and\n`OnDetachingFrom` methods, which should be overridden to provide logic that will be executed when\nthe behavior is attached to and detached from controls.</t>'}, {'line': 1009, 'html': '<t>` class derives from the\n`Behavior<t>` class. The purpose of the `BindableBehavior<t>` class is to provide a base class for\nXamarin.Forms behaviors that require the `BindingContext` of the behavior to be set to the attached\ncontrol.</t></t></t>'}, {'line': 1009, 'html': '<t>` class. The purpose of the `BindableBehavior<t>` class is to provide a base class for\nXamarin.Forms behaviors that require the `BindingContext` of the behavior to be set to the attached\ncontrol.</t></t>'}, {'line': 1009, 'html': '<t>` class is to provide a base class for\nXamarin.Forms behaviors that require the `BindingContext` of the behavior to be set to the attached\ncontrol.</t>'}, {'line': 1015, 'html': '<t>` class provides an overridable `OnAttachedTo` method that sets the\n`BindingContext` of the behavior, and an overridable `OnDetachingFrom` method that cleans up the\n`BindingContext` . In addition, the class stores a reference to the attached control in the\n`AssociatedObject` property.</t>'}, {'line': 1021, 'html': '<view>`\nclass so that the behavior can bind to and execute an `ICommand` specified by a `Command` property\nwhen the behavior is consumed. The following code example shows the `EventToCommandBehavior`\nclass:</view>'}, {'line': 1895, 'html': '<t>`, and each\n`ValidatableObject<t>` instance has validation rules added to its `Validations` property. Validation\nis invoked from the view model by calling the `Validate` method of the `ValidatableObject<t>`</t></t></t>'}, {'line': 1895, 'html': '<t>` instance has validation rules added to its `Validations` property. Validation\nis invoked from the view model by calling the `Validate` method of the `ValidatableObject<t>`</t></t>'}, {'line': 1895, 'html': '<t>`</t>'}, {'line': 1903, 'html': '<t>`\n`Value` property. Any validation errors are placed into the `Errors` property of the\n`ValidatableObject<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t></t>'}, {'line': 1903, 'html': '<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t>'}, {'line': 1903, 'html': '<t>` instance\nis updated to indicate whether validation succeeded or failed.</t>'}, {'line': 1909, 'html': '<t>` instance in the view model class\nto be notified of whether or not the entered data is valid.</t>'}, {'line': 1915, 'html': '<t>` interface,\nwhich is shown in the following code example:</t>'}, {'line': 1924, 'html': '<t>` validation rule, which is used to\nperform validation of the username and password entered by the user on the `LoginView` when using\nmock services in the eShopOnContainers mobile app:</t>'}, {'line': 1949, 'html': '<t>,` where `T` is the type of the data to be validated. The following\ncode example shows an example of two such properties:</t>'}, {'line': 1954, 'html': '<t>` instance, as demonstrated in the following code example:</t>'}, {'line': 1961, 'html': "<t>` validation rule to the `Validations` collection of\neach `ValidatableObject<t>` instance, specifying values for the validation rule's `ValidationMessage`\nproperty, which specifies the validation error message that will be displayed if validation fails.</t></t>"}, {'line': 1961, 'html': "<t>` instance, specifying values for the validation rule's `ValidationMessage`\nproperty, which specifies the validation error message that will be displayed if validation fails.</t>"}, {'line': 1978, 'html': '<t>` instance. The\nfollowing code example shows the `Validate` method from the `ValidatableObject<t>` class:</t></t>'}, {'line': 1978, 'html': '<t>` class:</t>'}, {'line': 1986, 'html': '<t>` instance. Finally, the `IsValid` property\nis set, and its value is returned to the calling method, indicating whether validation succeeded or\nfailed.</t>'}, {'line': 1999, 'html': "<t>` instance,\nand the control's `Behaviors` collection has an `EventToCommandBehavior` instance added to it. This\nbehavior executes the `ValidateUserNameCommand` in response to the `TextChanged` event firing on\nthe `Entry,` which is raised when the text in the `Entry` changes. In turn, the\n`ValidateUserNameCommand` delegate executes the `ValidateUserName` method, which executes the\n`Validate` method on the `ValidatableObject<t>` instance. Therefore, every time the user enters a\ncharacter in the `Entry` control for the username, validation of the entered data is performed.</t></t>"}, {'line': 1999, 'html': '<t>` instance. Therefore, every time the user enters a\ncharacter in the `Entry` control for the username, validation of the entered data is performed.</t>'}, {'line': 2115, 'html': '<t>` class, and is of type `List<string>.` Because the\n`Errors` property can contain multiple validation errors, the `FirstValidationErrorConverter`\ninstance is used to retrieve the first error from the collection for display.</string></t>'}, {'line': 2115, 'html': '<string>.` Because the\n`Errors` property can contain multiple validation errors, the `FirstValidationErrorConverter`\ninstance is used to retrieve the first error from the collection for display.</string>'}, {'line': 2127, 'html': '<t>`, and each\n`ValidatableObject<t>` instance has validation rules added to its `Validations` property. Validation\nis invoked from the view model by calling the `Validate` method of the `ValidatableObject<t>`\ninstance, which retrieves the validation rules and executes them against the `ValidatableObject<t>`\n`Value` property. Any validation errors are placed into the `Errors` property of the\n`ValidatableObject<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t></t></t></t></t>'}, {'line': 2127, 'html': '<t>` instance has validation rules added to its `Validations` property. Validation\nis invoked from the view model by calling the `Validate` method of the `ValidatableObject<t>`\ninstance, which retrieves the validation rules and executes them against the `ValidatableObject<t>`\n`Value` property. Any validation errors are placed into the `Errors` property of the\n`ValidatableObject<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t></t></t></t>'}, {'line': 2127, 'html': '<t>`\ninstance, which retrieves the validation rules and executes them against the `ValidatableObject<t>`\n`Value` property. Any validation errors are placed into the `Errors` property of the\n`ValidatableObject<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t></t></t>'}, {'line': 2127, 'html': '<t>`\n`Value` property. Any validation errors are placed into the `Errors` property of the\n`ValidatableObject<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t></t>'}, {'line': 2127, 'html': '<t>` instance, and the `IsValid` property of the `ValidatableObject<t>` instance\nis updated to indicate whether validation succeeded or failed.</t></t>'}, {'line': 2127, 'html': '<t>` instance\nis updated to indicate whether validation succeeded or failed.</t>'}, {'line': 2852, 'html': '<base endpoint=""/>'}, {'line': 2852, 'html': '<base` `endpoint="">:5105/connect/token`, which responds with access, identity, and refresh tokens.</base`>'}, {'line': 2858, 'html': '<base endpoint=""/>'}, {'line': 3715, 'html': '<t>` method is a generic method where `T` is the type of the\nexpected exception. The argument passed to the `Assert.Throws<t>` method is a lambda expression\nthat will throw the exception. Therefore, the unit test will pass provided that the lambda expression\nthrows an `ArgumentException` .</t></t>'}, {'line': 3715, 'html': '<t>` method is a lambda expression\nthat will throw the exception. Therefore, the unit test will pass provided that the lambda expression\nthrows an `ArgumentException` .</t>'}, {'line': 3726, 'html': '<t>` class performs as expected.</t>'}, {'line': 3736, 'html': '<t>` properties in the\n`MockViewModel` instance both have data.</t>'}, {'line': 3740, 'html': '<t>` instance, to verify that the\nclass performs as expected. The following code example demonstrates a unit test that does this:</t>'}, {'line': 3748, 'html': '<t>` instance\nare correctly set.</t>'}]