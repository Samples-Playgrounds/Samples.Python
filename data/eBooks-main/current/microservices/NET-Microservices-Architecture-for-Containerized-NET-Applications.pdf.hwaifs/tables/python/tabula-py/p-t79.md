|    | // EF Core DbContext                                                            |
|---:|:--------------------------------------------------------------------------------|
|  0 | public class OrderingContext : DbContext, IUnitOfWork                           |
|  1 | {                                                                               |
|  2 | // ...                                                                          |
|  3 | public async Task<bool> SaveEntitiesAsync(CancellationToken cancellationToken = |
|  4 | default(CancellationToken))                                                     |
|  5 | {                                                                               |
|  6 | // Dispatch Domain Events collection.                                           |
|  7 | // Choices:                                                                     |
|  8 | // A) Right BEFORE committing data (EF SaveChanges) into the DB. This makes     |
|  9 | // a single transaction including side effects from the domain event            |
| 10 | // handlers that are using the same DbContext with Scope lifetime               |
| 11 | // B) Right AFTER committing data (EF SaveChanges) into the DB. This makes      |
| 12 | // multiple transactions. You will need to handle eventual consistency and      |
| 13 | // compensatory actions in case of failures.                                    |
| 14 | await _mediator.DispatchDomainEventsAsync(this);                                |
| 15 | nan                                                                             |
| 16 | // After this line runs, all the changes (from the Command Handler and Domain   |
| 17 | // event handlers) performed through the DbContext will be committed            |
| 18 | var result = await base.SaveChangesAsync();                                     |
| 19 | }                                                                               |
| 20 | }                                                                               |