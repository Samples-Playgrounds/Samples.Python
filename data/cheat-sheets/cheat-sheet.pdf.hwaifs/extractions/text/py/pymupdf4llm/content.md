--print-out 

## Git Cheat Sheet 

|**Getting Started**<br>Start a new repo:<br>git init<br>Clone an existing repo:<br>git clone <url><br>**Prepare to Commit**<br>Add untracked file or unstaged changes:<br>git add <file><br>Add all untracked files and unstaged changes:<br>git add .<br>Choose which parts of a file to stage:<br>git add -p<br>Move file:<br>git mv <old> <new><br>Delete file:<br>git rm <file><br>Tell Git to forget about a file without deleting it:<br>git rm --cached <file><br>Unstage one file:<br>git reset <file><br>Unstage everything:<br>git reset<br>Check what you added:<br>git status<br>**Make Commits**<br>Make a commit (and open text editor to write<br>message):<br>git commit<br>Make a commit:<br>git commit -m 'message'<br>Commit all unstaged changes:<br>git commit -am 'message'<br>**Move Between Branches**<br>Switch branches:<br>git switch <name><br>**OR**<br>git checkout <name><br>Create a branch:<br>git switch -c <name><br>**OR**<br>git checkout -b <name><br>List branches:<br>git branch<br>List branches by most recently committed to:<br>git branch --sort=-committerdate<br>Delete a branch:<br>git branch -d <name><br>Force delete a branch:<br>git branch -D <name><br>**Diff Staged/Unstaged Changes**<br>Diff all staged and unstaged changes:<br>git diff HEAD<br>Diff just staged changes:<br>git diff --staged<br>Diff just unstaged changes:<br>git diff<br>**Diff Commits**<br>Show diff between a commit and its parent:<br>git show <commit><br>Diff two commits:<br>git diff <commit> <commit><br>Diff one file since a commit:<br>git diff <commit> <file><br>Show a summary of a diff:<br>git diff <commit> --stat<br>git show <commit> --stat<br>**Ways to refer to a commit**<br>Every time we say<commit>, you can use any of<br>these:<br>★ **a branch**<br>main<br>★ **a tag**<br>v0.1<br>★ **a commit ID**<br>3e887ab<br>★ **a remote branch**<br>origin/main<br>★ **current commit**<br>HEAD<br>★ **3 commits ago**<br>HEAD^^^ or HEAD~3<br>**Discard Your Changes**<br>Delete unstaged changes to one file:<br>git restore <file><br>**OR**<br>git checkout <file><br>Delete all staged and unstaged changes to one<br>file:<br>git restore --staged --worktree <file><br>**OR**<br>git checkout HEAD <file><br>Delete all staged and unstaged changes:<br>git reset --hard<br>Delete untracked files:<br>git clean<br>'Stash' all staged and unstaged changes:<br>git stash<br>**Edit History**<br>"Undo" the most recent commit (keep your<br>working directory the same):<br>git reset HEAD^<br>Squash the last 5 commits into one:<br>git rebase -i HEAD~6<br>Then change "pick" to "fixup" for any commit you<br>want to combine with the previous one<br>Undo a failed rebase:<br>git reflog BRANCHNAME<br>Then manually find the right commit ID in the<br>reflog, then run:<br>git reset --hard <commit><br>Change a commit message (or add a file you<br>forgot):<br>git commit --amend<br>**Code Archaeology**<br>Look at a branch's history:<br>git log main<br>git log --graph main<br>git log --oneline<br>Show every commit that modified a file:<br>git log <file><br>Show every commit that modified a file, including<br>before it was renamed:<br>git log --follow <file><br>Find every commit that added or removed some<br>text:<br>git log -G banana<br>Show who last changed each line of a file:<br>git blame <file><br>~~-A~~E|
|---|



**==> picture [771 x 542] intentionally omitted <==**

**----- Start of picture text -----**<br>
Bring a branch up to date with another branch<br>Combine Diverged Branches (aka "fast-forward merge"): Push Your Changes Important Files<br>Combine with rebase: git switch main Push the Local git config:<br>git switch banana main .git/config<br>git merge banana<br>branch to the remote<br>git rebase main Before: origin Global git config:<br>Before: A B C D E : ~/.gitconfig<br>A B C main git push origin main List of files to ignore:<br>.gitignore<br>Push the current branch to its remote "tracking<br>main banana branch":<br>D E banana git push<br>After:<br>Push a branch that you've never pushed before:<br>After: A B C D E main git push -u origin <name><br>main banana<br>Force push:<br>banana git push --force-with-lease<br>A B C D′ E′ Copy one commit onto the current branch: Push tags:<br>git push --tags<br>git cherry-pick <commit><br>Before:<br>D E "lost"<br>A B C main Pull Changes<br>Combine with merge: Fetch changes (but don't change any of your<br>git switch main local branches):<br>git merge banana D E git fetch origin main<br>Before: Fetch changes and then rebase your current<br>After: branch:<br>A B C main git pull --rebase<br>A B C D′ main<br>Fetch changes and then merge them into your<br>current branch:<br>D E banana git pull origin main<br>D E<br>OR<br>After:<br>git pull<br>A B C ◇ main<br>Restore an Old File<br>Get the version of a file from another commit: Configure Git<br>D E banana git checkout <commit> <file> Set a config option:<br>OR git config user.name 'Your Name'<br>Combine with squash merge:<br>git switch main git restore <file> --source <commit> Set option globally:<br>git config --global ...<br>git merge --squash banana<br>Add an alias:<br>git commit Add a Remote git config alias.st status<br>Before: git remote add <name> <url><br>See all possible config options:<br>A B C main man git-config<br>D E banana<br>After:<br>D<br>A B C main<br>E<br>D E banana<br>**----- End of picture text -----**<br>


