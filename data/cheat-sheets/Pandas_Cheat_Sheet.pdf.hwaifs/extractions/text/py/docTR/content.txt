Tidy Data - A foundation for wrangling in pandas
Data Wrangling
F M A
F M A Tidy data complements pandas's vectorized M
A
F
with pandas Cheat Sheet
operations. pandas will automatically preserve
In a tidy
http://pandas.pydata.org
data set: AAf
observations as you manipulate variables. No
other format works as intuitively with pandas.
Pandas API Reference Pandas User Guide
Each variable is saved
Each observation is
M * A
in its own column
saved in its own row
Creating DataFrames
Reshaping Data = Change layout, sorting, reindexing, renaming
df.sort values('mpg')
10
Order rows by values of a column (low to high).
11
df.sort values('mpg', ascending-False)
12
Order rows by values of a column (high to low).
df = pd.DataFrame(
("a" : [4, 5, 6],
pd.melt(df)
df.pivot(columns='var", values='val") df.rename(columns = ('y':'year'))
Gather columns into rows.
"b" : [7, 8, 9],
Spread rows into columns.
Rename the columns of al DataFrame
"c" : [10, 11, 12]),
=
index [1, 2, 3])
df.sort index()
Sort the index of a DataFrame
Specify values for each column.
df.reset index()
df = pd.DataFrame(
Reset index of DataFrame to row numbers, moving
[[4, 7, 10],
index to columns.
[5, 8, 11],
pd.concat([df1,df2])
pd.concat([df1,df2), axis=1)
[6, 9, 12]1,
df.drop(columns=['Length ', 'Height'])
Append rows of DataFrames
Append columns of DataFrames
index=[1, 2, 3],
Drop columns from DataFrame
columns=['a', 'b', 'c'])
Specify values for each row.
Subset Observations - rows
Subset Variables - columns
Subsets - rows and columns
Use df.loc[] and df.iloc[] to select only
N
rows, only columns or both.
10
Use df.at[] and df.iat[] to access a
D
single
11
df[df.Length > 7]
dffI'width', 'length', 'species 11
valuel by row and column.
Extract rows that meet logical criteria.
Select multiple columns with
2
12
specific names. First index selects rows, second index columns.
df.drop duplicates()
df[ 'width'] or df.width
df = pd.DataFrame(
Remove duplicate rows (only considers columns). Select: single column with specific name.
df.iloc(10:20] Select rows 10-20.
f"a" : [4 ,5, 6],
df.sample(frac=0.5)
df.filter(regex='regex")
df.iloc[:, 2, 5]]
"b" : [7, 8, 9],
Randomly select fraction of rows.
Select columns whose name matches
[1,
"c"
rows.
Select columns in positions 1, 2 and 5 (first
: [10, 11, 12]),
df.sample(n-10) Randomly select n
regular expression regex.
column is 0).
index = pd.MultiIndex.from tuples(
df.nlargest(n, 'value')
df.loc[:, 'x2':'x4']
[('d', 1), ('d', 2),
Select and order top n entries.
Using query
Select all columns between x2 and x4 (inclusive).
('e', 2)], names=I'n', 'v'])) df.nsmallest(n, 'value')
allows Boolean
for
Create DataFrame with al Multilndex
Select and order bottom n entries.
queryl)
expressions filtering df.loc(df['a'] >> 10, ['a', 'c']]
df.head(n)
rows.
Select rows meeting logical condition, and only
Select first n rows.
df.query("Length > 7')
the specific columns.
Method Chaining
df.tail(n)
df querv('Length > 7 and Width < 8') df.iat[1, 2] Access single value by index
Select last n rows.
df .query( 'Name.str. startswith("abc")", df.at[4, 'A'] Access single value by label
Most pandas methods return al DataFrame SO that
engine-"python"
another pandas method can be applied to the result.
Logic in Python (and pandas)
regex (Regular Expressions) Examples
This improves readability of code.
df = (pd.melt(df)
< Less than
!=
Not equal to
'l.'
Matches strings containing ap period :
rename(columns-(
> Greater than
df.colum.isin(values) Group membership
'Length$'
Matches strings ending with word' 'Length'
'variable':'var',
== Equals
pd.isnull(obj)
IsNaN
"ASepal'
Matches strings beginning with the word 'Sepal'
'value':'val"))
<= Lesst than or equals pd. notnull(obj)
Isnot NaN
"Ax(1-5]$'
Matches strings beginning with 'x' and ending with 1,2,3,4,5
querv('val >= 200')
>= Greater than ore requals 8,l-,",df.any0),df.al10) Logical and, or,not, xor, any, all "(P1Speciess).** Matches strings except the string' "Species'
Cheatsheetforp pandas (https/pandas.pydata.orel originally written bylrvL Lustig, Princeton Consultants, inspired byRstudio Data Wranglin ne Cheatsheet



Group Data
Combine Data Sets
df.groupby(by-"col")
The examples below can also be applied to groups. In this case, the
Return a GroupBy object, grouped function is applied on a per-group basis, and the returned vectors
adf
bdf
by values in column named "col". are of the length of the original DataFrame.
x1 x2
x1x3
A 1
A T
shift(1)
shift(-1)
B 2
B F
df.groupby(level-"ind")
Copy with values shifted by 1. Copy with values lagged by 1.
C 3
D T
Return a GroupBy object, grouped rank(method=" 'dense')
cumsum()
by values in index level named
Ranks with no gaps.
Cumulative sum.
StandardJoins
"ind".
rank(method= 'min' ')
cummax()
x1 x2 x3 pd.merge(adf, bdf,
Ranks. Ties get min rank.
Cumulative max.
A 1 T
how-'left', on='x1 )
All of the summary functions listed above can be applied to a group. rank(pct-True)
cummin()
B 2 F Join matching rows from bdf to adf.
Additional GroupBy functions:
Ranks rescaled to interval [0, 1]. Cumulative min.
C 3 NaN
size()
agg(function)
rank(method-"first")
cumprod()
Size of each group.
Aggregate group using function. Ranks. Ties go to first value.
Cumulative product.
x1 x2 x3 pd.merge(adf, bdf,
A 1.0 T
how='right', on='x1')
Summarize Data
Handling Missing Data
B 2.0 F Join matching rows from adf to bdf.
D NaN T
df['w'].value counts()
df.dropna()
Count number of rows with each unique value of variable
Drop rows with any column having NA/null data.
x1 x2 x3 pd.merge(adf, bdf,
len(df)
df.fillna(value)
A 1 T
how='inner', on='x1')
# of rows in DataFrame.
Replace all NA/null data with value.
B 2 F Join data. Retain only rows in both sets.
df.shape
Tuple of # of rows, # of columns in DataFrame.
Make New Columns
x1 x2 x3 pd.merge(adf, bdf,
dfl'w'1.nunique()
A 1 T
how='outer', on='x1')
# of distinct values in a column.
B 2 F Join data. Retain all values, all rows.
df.describe()
C 3 NaN
Basic descriptive and statistics for each column (or GroupBy).
D NaN T
df.info()
FilteringJoins
Prints a concise summary of the DataFrame.
df.assign(Area-lambda df: df.Length*df.Height)
x2
adf[adf.x1.isin(bdf.x1)]
df.memorv usage()
Compute and append one or more new columns.
A 1
All rows in adf that have a match in bdf.
Prints the memory usage of each column in the DataFrame.
df[ 'Volume '] = df. Length*df.Height*df.Depth
B 2
df.dtypes()
Add single column.
Prints a Series with the dtype of each column in the DataFrame.
pd.gcut(df.col, n, labels=False)
1x2
adfl-adf.x1.isin(bdf.x1)]
Bin column into n buckets.
C 3
All rows in adf that do not have a match in bdf
Vector
Vector
ydf
zdf
function
function
1x2
x1
pandas provides a large: set of summary functions that operate on
A 1
B 2
different kinds of pandas objects (DataFrame columns, Series,
pandas provides a large: set of vector functions that operate on all
B 2
C 3
GroupBy, Expanding and Rolling (see below)) and produce single
columns of a DataFrame or a single selected column (a pandas
C 3
D 4
values for each of the groups. When applied to a DataFrame, the
Series). These functions produce vectors of values for each of the
Set-like Operations
result is returned as a pandas Series for each column. Examples:
columns, or a single Series for the individual Series. Examples:
x1x2
pd.merge(ydf,
zdf)
sum()
min()
max(axis=1)
min(axis=1)
B 2
Rows that in both and zdf
Sum values of each object. Minimum value in each object.
Element-wise max.
Element-wise min.
C 3
(Intersection). appear ydf
count()
max()
clin(lower=-10, upper=10) abs()
Count non-NA/null values of Maximum value in each object. Trim values at input thresholds Absolute value.
x1x2
each object.
mean()
A 1
pd.merge(ydf, zdf, how='outer')
Rows that appear in either or both ydf and zdf
median()
Mean value of each object.
B 2
Median value of each object. var()
Windows
C 3
(Union).
quantile((0.25,0.75]) Variance of each object.
df.expanding()
D 4
pd.merge(ydf, zdf, how='outer',
Quantiles of each object. std()
Return an Expanding object allowing summary functions to be
indicator=True)
apply(function)
Standard deviation of each
applied cumulatively.
x1x2
Apply function to each object. object.
df.rolling(n)
A 1
.querv('. _merge == "left_only"")
dron(columns=I'_merge'])
Return a Rolling object allowing summary functions to be
Rows that appear in ydf but not zdf (Setdiff).
applied to windows of length n.
Cheatsheeti forp pandas (http:/ data.org/) orginallywittenbylvu Lustig, Princeton.Consltants inspiredb byRstudio.DataWran



Plotting
Frequently Used
df.plot()
df.plot.scatter(x='w", y='h')
df.plot.hist()
df.plot.pie0)
Plot a line for the DataFrame. Plot a
the
Options
graph
scatter graph of DataFrame.
Plot a histogram of the DataFrame. Plot a pie chart of the DataFrame.
Pandas offers some 'options' to globally
control how Pandas behaves, display etc.
Options can be queried and set via:
df.plot.bar0)
df.plot.boxplot0)
df.plot.area0)
df-plot.hexbin0)
pd.options.option_name (where
Plot al line graph for the DataFrame. Plot as scatter graph of the DataFrame.
Plot an area graph of the DataFrame. Plot al hexbin graph of the DataFrame. option_namei is the name of an option). For
example:
pd.options.display. max_rows = 20
I
Set the display.max_rows option to 20.
df.plot(subplots-True)
df.plot(cumulative-True)
df.plot(stacked-True)
Functions
Separate into different graphs for each column in
Creates a cumulative plot
Stacks the data for the columns on top of each
get option(option)
the DataFrame.
df.plot(bins-30)
other. (bar, barh and area only)
Fetch the value of the given option.
df.plot(title-"Graph of A against B")
Set the number of bins into which data is grouped df.plot(alpha=0.5)
set option(option)
Sets the title of the graph.
(histograms)
Sets the transparency oft the plot to 50%.
Set the value of the given option.
reset option(options)
df.plot(subplots-True, title-l'coll', 'col2', 'co13'])
Reset the values of all
to
Arguments can be combined for more flexibility when graphing, this would plot a separate line graph for of column of a 3-columned DataFrame. The first string in the
given options
list
default
of titles applies to the graph of the left-most column.
settings.
describe option(options)
Print descriptions of given options.
Changing Type
Series String Operations
option context(options)
Execute code with temporary option
Similar to python string operations, except these are vectorized to apply to the
pd.to numeric(data)
df.astype(type)
settings that revert to prior settings after
entire Series efficiently.
Convert non-numeric types to
Convert data to (almost) any given 5.str.count(pattern)
s.str.cat()
execution.
numeric.
type including categorical
Returns a: series with the integer
Concatenate elements into a: single Displayoptions
pd.to datetime(data)
df.infer objects()
counts in each element.
string
display.max_rows
Convert non-datetime types to
Attempts to infer a better type for S.str.get(index)
s.str.partition(sep)
The maximum number of rows displayed
datetime type
object type data.
Returns a: series with the data at the Splits the string on the first instance in pretty-print.
pd.to timedelta(data)
df.convert dtypes()
given index for each element.
ofthe separator
display.max_columns
Convert non- timedelta types to
Convert columns to best possible
s.str.join(sep)
5.str.slice(start, stop,
The maximum number of columns
timedelta
dtypes
Returns a: series where each element step)
displayed in pretty-print.
has been concatenated.
Slices each string
display. expand_frame_repr
Datetime
s.str.title()
s.str.replace(pat, rep)
Controls whether the DataFrame
s.dt.day Extract the day (int) from the date.
Converts the first character of each Use regex to replace patterns in each representation stretches across pages.
With as Series containing data oftype
s.dt.quarter
word to be a capital.
string.
display.large_repr
datetime, the dt accessor is used to get
Find which quarter the date lies in. s.str.len()
s.str.isalnum()
Controls whether a DataFrame that
various components oft the datetime
s.dt.hour
Returns a series with the lengths of Checks whether each element is
exceeds maximum rows/columns is
values:
each element.
alpha-numeric
truncated or summarized
Extract the hour.
s.dt.year
s.dt.minute
display.precision
Extract the year
The output display precision in decimal
Extract the minute.
Input/Output
s.dt.month
places.
s.dt.second
Extract the month as an integer.
Common file types for data input include CSV, JSON, HTML which are human-
display.max.colwidth
Extracti the second.
readable, while the common output types are usually more optimized for
The maximum width of columns, longer
performance and scalability such as feather, parquet and HDF.
cells will be truncated.
Mapping
df = pd.read csu(filepath) df.to parquet(Filepath)
display.max_info_columns
Read data from CSV file
Write data to parquet file
The maximum number of columns
Apply a mapping to every element in a DataFrame or Series, useful for
df = pd.read html (filepath) df.to feather(filepath)
displayed after calling info().
recategorizing or transforming data.
Read data from html file
Write data to feather file
display.chop_threshold
df = pd.read excel(filepath) df.to hdf(filepath)
Sets the rounding threshold to zero when
s.map(lambda X: 2*x)
Returns a copy oft the series where every entryi is doubled
Read data from xls (and related) files Write data to HDF file
displaying a Series/Dataframe.
df = pd.read sal (filepath) df.to clipboard()
display.colheader. justify
df.apply(lambda s: s.max() s.min(), axis=1)
Read data from sql file
Copy object to the clipboard
Controls how column headers are justified.
Returns a Series with the difference of the maximum and minimum values of
system
each row ofthe DataFrame
pd.read clipboard()
Read text from clipboard